---
/**
 * Hindi Blog Index - Blog listing in Hindi
 */
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

console.log('[BlogIndex Hindi] Loading blog posts...');

// Get all blog posts and sort by date (newest first)
const allBlogPosts = await getCollection('blog');
const sortedPosts = allBlogPosts.sort((a, b) => 
  b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

console.log('[BlogIndex Hindi] ✓ Loaded', sortedPosts.length, 'blog posts');

// Extract unique tags
const allTags = [...new Set(sortedPosts.flatMap(post => post.data.tags || []))];

const pageTitle = 'आदि कैलाश यात्रा ब्लॉग - गाइड, टिप्स और कहानियां';
const pageDescription = 'अपनी आदि कैलाश यात्रा के लिए व्यापक यात्रा गाइड, तीर्थयात्रा सुझाव और आध्यात्मिक अंतर्दृष्टि प्राप्त करें।';
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  pageName="blog"
>
  <!-- Hero Section with Spiritual Design -->
  <section class="relative bg-gradient-to-br from-primary-600 via-accent-500 to-primary-700 text-white py-20 md:py-28 overflow-hidden">
    <!-- Decorative Om Symbol -->
    <div class="absolute top-10 right-10 text-white/10 text-9xl font-bold select-none pointer-events-none">
      ॐ
    </div>
    <div class="absolute bottom-10 left-10 text-white/10 text-9xl font-bold select-none pointer-events-none">
      ॐ
    </div>
    
    <!-- Floating Lotus Petals Animation -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <div class="lotus-petal" style="left: 10%; animation-delay: 0s;">🌸</div>
      <div class="lotus-petal" style="left: 30%; animation-delay: 2s;">🌺</div>
      <div class="lotus-petal" style="left: 50%; animation-delay: 4s;">🏵️</div>
      <div class="lotus-petal" style="left: 70%; animation-delay: 1s;">🌸</div>
      <div class="lotus-petal" style="left: 90%; animation-delay: 3s;">🌺</div>
    </div>
    
    <div class="container-custom relative z-10 text-center">
      <div class="inline-block mb-4 px-6 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-medium">
        📚 यात्रा ज्ञान और आध्यात्मिक अंतर्दृष्टि
      </div>
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6">
        आदि कैलाश यात्रा ब्लॉग
      </h1>
      <p class="text-lg md:text-xl opacity-90 max-w-3xl mx-auto leading-relaxed">
        पवित्र आदि कैलाश तीर्थयात्रा के लिए आपकी संपूर्ण गाइड। विशेषज्ञ सुझाव, आध्यात्मिक अंतर्दृष्टि और आपकी यात्रा के लिए व्यावहारिक सलाह।
      </p>
      
      <!-- Search Bar (Placeholder) -->
      <div class="mt-10 max-w-2xl mx-auto">
        <div class="relative">
          <input
            type="search"
            placeholder="लेख खोजें..."
            class="w-full px-6 py-4 rounded-full text-gray-800 shadow-2xl border-4 border-white/30 focus:border-white focus:outline-none text-lg"
          />
          <button class="absolute right-2 top-1/2 -translate-y-1/2 bg-primary-600 hover:bg-primary-700 text-white px-6 py-2 rounded-full transition-colors">
            🔍 खोजें
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- Blog Posts Grid -->
  <section class="py-16 md:py-20 bg-gray-50">
    <div class="container-custom">
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {sortedPosts.map((post) => (
          <article class="group bg-white rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl transition-all duration-300 border-2 border-gray-100 hover:border-primary-300">
            <a href={`/hi/blog/${post.slug}`} class="block">
              <div class="p-6">
                {post.data.tags && post.data.tags.length > 0 && (
                  <div class="mb-3">
                    <span class="inline-block px-3 py-1 bg-primary-100 text-primary-700 rounded-full text-xs font-semibold">
                      {post.data.tags[0]}
                    </span>
                  </div>
                )}
                <h2 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-primary-600 transition-colors">
                  {post.data.title}
                </h2>
                <p class="text-gray-600 mb-4 line-clamp-3">
                  {post.data.description}
                </p>
                <div class="flex items-center justify-between text-sm text-gray-500">
                  <time datetime={post.data.publishDate}>
                    {new Date(post.data.publishDate).toLocaleDateString('hi-IN')}
                  </time>
                  <span class="text-primary-600 font-semibold group-hover:translate-x-1 transition-transform">
                    और पढ़ें →
                  </span>
                </div>
              </div>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  @keyframes lotus-float {
    0%, 100% {
      transform: translateY(100vh) rotate(0deg);
      opacity: 0;
    }
    10% {
      opacity: 1;
    }
    90% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100px) rotate(360deg);
      opacity: 0;
    }
  }

  .lotus-petal {
    position: absolute;
    bottom: -50px;
    font-size: 2rem;
    animation: lotus-float 15s infinite;
  }
</style>


---
/**
 * Hindi Blog Post - Uses same component logic with Hindi locale
 */
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getCollection, type CollectionEntry } from 'astro:content';
import { getWhatsAppUrl } from '../../../utils/whatsapp';

// Get all blog posts - same logic as English
export async function getStaticPaths() {
  const blogPosts = await getCollection('blog');
  return blogPosts.map(post => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { Content } = await post.render();

console.log('[BlogPost Hindi] Rendering:', post.slug);

// Get related posts (same tags)
const allPosts = await getCollection('blog');
const relatedPosts = allPosts
  .filter(p => 
    p.slug !== post.slug && 
    p.data.tags?.some(tag => post.data.tags?.includes(tag))
  )
  .slice(0, 3);

console.log('[BlogPost Hindi] ✓ Found', relatedPosts.length, 'related posts');

// Estimate reading time (assuming 200 words per minute)
const contentString = JSON.stringify(post.body);
const wordCount = contentString.split(/\s+/).length;
const readingTime = Math.ceil(wordCount / 200);

const whatsappURL = getWhatsAppUrl({ 
  message: `नमस्ते, मैंने आपका लेख "${post.data.title}" पढ़ा और मुझे आदि कैलाश यात्रा में रुचि है। कृपया अधिक विवरण साझा करें।`
});
---

<BaseLayout
  title={post.data.title}
  description={post.data.description}
  pageName="blog"
>
  <!-- Hero Section with Featured Image -->
  <article class="blog-post-content">
    <header class="relative bg-gradient-to-br from-secondary-900 via-primary-800 to-accent-700 text-white py-16 md:py-24 overflow-hidden">
      <!-- Decorative Background Pattern -->
      <div class="absolute inset-0 opacity-10">
        <div class="absolute top-0 left-0 text-9xl">🕉️</div>
        <div class="absolute top-1/4 right-0 text-9xl">🌸</div>
        <div class="absolute bottom-0 left-1/4 text-9xl">🏔️</div>
        <div class="absolute bottom-1/4 right-1/4 text-9xl">📿</div>
      </div>
      
      <div class="container-custom relative z-10">
        <!-- Breadcrumb -->
        <nav class="text-sm mb-6 flex items-center gap-2 text-white/80">
          <a href="/hi" class="hover:text-white transition-colors">होम</a>
          <span>→</span>
          <a href="/hi/blog" class="hover:text-white transition-colors">ब्लॉग</a>
          <span>→</span>
          <span class="text-white font-medium">{post.data.title.substring(0, 30)}...</span>
        </nav>

        <div class="max-w-4xl">
          <!-- Category Badge -->
          {post.data.tags && post.data.tags.length > 0 && (
            <div class="mb-4">
              <span class="inline-block px-4 py-2 bg-white/20 backdrop-blur-sm rounded-full text-sm font-semibold">
                📚 {post.data.tags[0]}
              </span>
            </div>
          )}

          <!-- Title -->
          <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6 leading-tight">
            {post.data.title}
          </h1>

          <!-- Meta Info -->
          <div class="flex flex-wrap items-center gap-6 text-white/90 text-sm md:text-base">
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
              </svg>
              <time datetime={post.data.publishDate}>{new Date(post.data.publishDate).toLocaleDateString('hi-IN')}</time>
            </div>
            <div class="flex items-center gap-2">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"/>
              </svg>
              <span>{readingTime} मिनट पढ़ें</span>
            </div>
          </div>
        </div>
      </div>
    </header>

    <!-- Article Content -->
    <div class="container-custom py-12 md:py-16">
      <div class="max-w-4xl mx-auto">
        <div class="prose prose-lg max-w-none">
          <Content />
        </div>

        <!-- WhatsApp CTA -->
        <div class="mt-12 p-6 bg-gradient-to-r from-green-50 to-blue-50 rounded-xl border-2 border-green-200">
          <div class="flex items-center gap-4">
            <div class="text-5xl">📞</div>
            <div class="flex-1">
              <h3 class="text-xl font-bold text-gray-900 mb-2">
                आदि कैलाश यात्रा के बारे में अधिक जानकारी चाहिए?
              </h3>
              <p class="text-gray-600 mb-4">
                हमारे विशेषज्ञों से सीधे बात करें और अपनी यात्रा की योजना बनाएं!
              </p>
              <a 
                href={whatsappURL}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-2 px-6 py-3 bg-[#25D366] text-white rounded-lg font-semibold hover:bg-[#128C7E] transition-colors"
              >
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
                </svg>
                व्हाट्सएप पर संपर्क करें
              </a>
            </div>
          </div>
        </div>

        <!-- Related Posts -->
        {relatedPosts.length > 0 && (
          <div class="mt-16">
            <h2 class="text-2xl md:text-3xl font-bold text-gray-900 mb-8">
              संबंधित लेख
            </h2>
            <div class="grid md:grid-cols-3 gap-6">
              {relatedPosts.map(relatedPost => (
                <a 
                  href={`/hi/blog/${relatedPost.slug}`}
                  class="group block p-6 bg-white rounded-xl border-2 border-gray-200 hover:border-primary-500 hover:shadow-xl transition-all"
                >
                  <h3 class="font-bold text-lg text-gray-900 mb-2 group-hover:text-primary-600 transition-colors">
                    {relatedPost.data.title}
                  </h3>
                  <p class="text-gray-600 text-sm mb-4 line-clamp-3">
                    {relatedPost.data.description}
                  </p>
                  <span class="text-primary-600 font-semibold text-sm">
                    और पढ़ें →
                  </span>
                </a>
              ))}
            </div>
          </div>
        )}
      </div>
    </div>
  </article>
</BaseLayout>


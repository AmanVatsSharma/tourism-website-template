---
/**
 * Gallery Page - BEAUTIFUL IMAGE GALLERY! 📸
 * 
 * Features all the stunning spiritual images from public/images folder
 * 
 * ROUTING: /gallery (English), /hi/gallery (Hindi)
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import { detectLanguage } from '../utils/languageHelper';
import { useTranslation } from '../utils/translations';

// Detect language
const langContext = detectLanguage(Astro);
const locale = langContext.locale;
const t = useTranslation(locale);

console.log('[GalleryPage] ========================================');
console.log('[GalleryPage] Loading gallery...');
console.log('[GalleryPage] Locale:', locale);

const pageTitle = t('gallery.heading', 'Photo Gallery - Adi Kailash Yatra Images');
const pageDescription = t('gallery.subheading', 'Explore breathtaking images from Adi Kailash pilgrimage');

// Beautiful images from public/images folder
const galleryImages = [
  { src: '/images/adi-kailash-parvat.webp', alt: 'Adi Kailash Mountain Peak', category: 'peaks' },
  { src: '/images/Adi-Kailash.webp', alt: 'Adi Kailash Sacred Mountain', category: 'peaks' },
  { src: '/images/6d25384b-482d-473c-98c6-d65e9853ad99_Adi-Kailash 1.webp', alt: 'Adi Kailash View', category: 'peaks' },
  { src: '/images/adi kailash trek_1684871793.webp', alt: 'Adi Kailash Trekking', category: 'journey' },
  { src: '/images/adi-kailash-900x600-1-680f20db6fde196b9c984b63-45177620.webp', alt: 'Adi Kailash Panorama', category: 'peaks' },
  { src: '/images/kailashyatra1-1697183775.jpg', alt: 'Kailash Yatra Group', category: 'journey' },
  { src: '/images/narendra-modi-adi-kailash-visit-6452306.webp', alt: 'VIP Visit to Adi Kailash', category: 'journey' },
  { src: '/images/modi-parvati-kund-pooja_202310277471.jpg', alt: 'Parvati Kund Pooja Ceremony', category: 'temples' },
  { src: '/images/66a733e1d19a0455f7a69c0b-adi-kailash-temple.webp', alt: 'Adi Kailash Temple', category: 'temples' },
  { src: '/images/27_10_2023-adi_kailash_23566403.jpeg', alt: 'Adi Kailash Journey', category: 'journey' },
  { src: '/images/Adi-Kailash--819x1024.jpeg', alt: 'Adi Kailash Portrait', category: 'peaks' },
  { src: '/images/adi-kailash-6306783.webp', alt: 'Adi Kailash Summit', category: 'peaks' },
  { src: '/images/c8b7e532-80b4-40f7-845f-87a4fb581461_Adi-Kailash-3 (1).webp', alt: 'Adi Kailash Landscape', category: 'peaks' },
  { src: '/images/Adi-Kailash.jpg', alt: 'Adi Kailash Mountain', category: 'peaks' },
  { src: '/images/adi_kailash_tour16.jpg', alt: 'Adi Kailash Tour', category: 'journey' },
];

// Categories
const categories = [
  { id: 'all', name: t('gallery.all', 'All Photos'), icon: '🖼️' },
  { id: 'peaks', name: t('gallery.peaks', 'Mountains & Peaks'), icon: '🏔️' },
  { id: 'temples', name: t('gallery.temples', 'Temples'), icon: '🛕' },
  { id: 'journey', name: t('gallery.journey', 'Journey'), icon: '🚗' },
];

console.log('[GalleryPage] ✓ Ready with', galleryImages.length, 'images');
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  pageName="gallery"
>
  <section class="bg-gradient-to-br from-secondary-900 to-primary-900 text-white py-16 md:py-20">
    <div class="container-custom text-center">
      <div class="text-5xl mb-4">📸</div>
      <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-4">
        {t('gallery.heading', 'Photo Gallery')}
      </h1>
      <p class="text-base md:text-lg lg:text-xl opacity-90 max-w-3xl mx-auto">
        {t('gallery.subheading', 'Glimpses of the sacred journey to Adi Kailash')}
      </p>
    </div>
  </section>

  <!-- Category Filter -->
  <section class="py-8 bg-white sticky top-16 z-30 border-b border-gray-200">
    <div class="container-custom">
      <div class="flex flex-wrap justify-center gap-3 md:gap-4">
        {categories.map((cat) => (
          <button 
            class="filter-btn px-4 md:px-6 py-2 md:py-3 rounded-full bg-gray-100 hover:bg-primary-100 hover:text-primary-700 transition-colors font-medium text-sm md:text-base"
            data-category={cat.id}
          >
            <span class="mr-2">{cat.icon}</span>
            {cat.name}
          </button>
        ))}
      </div>
    </div>
  </section>

  <!-- Gallery Grid -->
  <section class="section bg-gray-50">
    <div class="container-custom">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 md:gap-6" id="galleryGrid">
        {galleryImages.map((img, index) => (
          <div class="gallery-item group cursor-pointer" data-category={img.category} data-index={index}>
            <div class="relative aspect-square rounded-xl overflow-hidden shadow-md hover:shadow-xl transition-all duration-300">
              <img 
                src={img.src}
                alt={img.alt}
                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                <div class="absolute bottom-0 left-0 right-0 p-4 text-white">
                  <div class="text-3xl mb-2">
                    {img.category === 'peaks' ? '🏔️' : img.category === 'temples' ? '🛕' : '🚗'}
                  </div>
                  <p class="text-sm font-semibold">{img.alt}</p>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Note about placeholder images -->
  <section class="section bg-white">
    <div class="container-custom">
      <div class="max-w-3xl mx-auto text-center">
        <div class="bg-blue-50 border-2 border-blue-200 rounded-xl p-6 md:p-8">
          <div class="text-4xl md:text-5xl mb-4">📸</div>
          <h2 class="text-xl md:text-2xl font-bold text-secondary-800 mb-3">
            Image Placeholders
          </h2>
          <p class="text-sm md:text-base text-gray-700 mb-4">
            These are placeholder representations. Actual photos from Adi Kailash will be added to showcase the stunning beauty of this sacred pilgrimage.
          </p>
          <p class="text-xs md:text-sm text-gray-600">
            <strong>Want to see real photos?</strong> Contact us or visit our office for a complete photo album of previous yatras.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="section bg-gradient-to-r from-primary-600 to-accent-600 text-white">
    <div class="container-custom text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-4">
        Ready to Create Your Own Memories?
      </h2>
      <p class="text-base md:text-lg opacity-90 mb-6 md:mb-8 max-w-2xl mx-auto">
        Join us on this sacred journey and capture your own spiritual moments
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/packages" class="btn-secondary text-base md:text-lg px-6 md:px-8 py-3 md:py-4">
          Book Your Yatra
        </a>
        <a href="/contact" class="btn-outline border-white text-white hover:bg-white hover:text-primary-600 text-base md:text-lg px-6 md:px-8 py-3 md:py-4">
          Contact Us
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  // Simple category filter (client-side)
  document.addEventListener('DOMContentLoaded', () => {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const galleryItems = document.querySelectorAll('.gallery-item');
    
    filterBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const category = btn.getAttribute('data-category');
        
        // Update active button
        filterBtns.forEach(b => b.classList.remove('bg-primary-500', 'text-white'));
        btn.classList.add('bg-primary-500', 'text-white');
        
        // Filter items (show all for now - can be enhanced)
        galleryItems.forEach(item => {
          item.style.display = 'block';
        });
        
        console.log(`[Gallery] Filtered to category: ${category}`);
      });
    });
    
    console.log('[Gallery] Initialized with', galleryItems.length, 'images');
  });
</script>


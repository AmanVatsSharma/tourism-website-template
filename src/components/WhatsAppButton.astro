---
/**
 * WhatsApp Floating Button Component
 * 
 * Features:
 * - Fixed position button (bottom-right corner)
 * - Pulse animation for attention
 * - Hover effects with spiritual glow
 * - Pre-filled message based on current page
 * - Mobile-optimized size
 * - Accessible with ARIA labels
 */

import { getWhatsAppUrl, whatsappMessages } from '../utils/whatsapp';
import { useTranslation } from '../utils/translations';

interface Props {
  currentLocale: 'en' | 'hi';
}

const { currentLocale } = Astro.props;
const t = useTranslation(currentLocale);

// Get the WhatsApp URL with default message
const whatsappURL = getWhatsAppUrl({ message: t('whatsapp.message') });

console.log('[WhatsAppButton] Rendering WhatsApp button');
---

<!-- WhatsApp Floating Button -->
<a
  href={whatsappURL}
  target="_blank"
  rel="noopener noreferrer"
  class="whatsapp-float fixed bottom-6 right-6 z-[60] group"
  aria-label={t('whatsapp.button')}
  id="whatsapp-float-btn"
>
  <!-- Pulse Ring Animation -->
  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-success opacity-75"></span>
  
  <!-- Button Container with Glow Effect -->
  <div class="relative flex items-center justify-center w-16 h-16 md:w-18 md:h-18 bg-gradient-to-br from-success to-green-600 rounded-full shadow-2xl transform transition-all duration-300 group-hover:scale-110 group-hover:shadow-3xl group-hover:rotate-12">
    <!-- WhatsApp Icon -->
    <svg class="w-8 h-8 md:w-10 md:h-10 text-white group-hover:scale-110 transition-transform duration-300" fill="currentColor" viewBox="0 0 24 24">
      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
    </svg>
    
    <!-- Spiritual Glow Effect on Hover -->
    <div class="absolute inset-0 rounded-full bg-gradient-to-r from-accent-400 to-primary-400 opacity-0 group-hover:opacity-30 blur-xl transition-opacity duration-300"></div>
  </div>
  
  <!-- Tooltip (Visible on Hover) -->
  <div class="absolute right-full top-1/2 -translate-y-1/2 mr-4 opacity-0 group-hover:opacity-100 transition-opacity duration-300 pointer-events-none">
    <div class="bg-white text-gray-800 px-4 py-2 rounded-lg shadow-xl whitespace-nowrap font-semibold flex items-center gap-2">
      <span>💬</span>
      <span>{t('whatsapp.button')}</span>
      <!-- Tooltip Arrow -->
      <svg class="absolute right-0 top-1/2 -translate-y-1/2 translate-x-full text-white" width="8" height="16" viewBox="0 0 8 16" fill="currentColor">
        <path d="M0 0L8 8L0 16V0Z"/>
      </svg>
    </div>
  </div>

  <!-- Decorative Om Symbol (Spiritual Touch) -->
  <div class="absolute -top-2 -right-2 text-xl opacity-0 group-hover:opacity-100 transition-opacity duration-300 animate-pulse">
    🕉️
  </div>
</a>

<style>
  /* Floating Animation (Subtle Bounce) */
  .whatsapp-float {
    animation: float-bounce 3s ease-in-out infinite;
  }

  @keyframes float-bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  /* Enhanced Shadow on Hover */
  .shadow-3xl {
    box-shadow: 0 25px 50px -12px rgba(37, 211, 102, 0.5);
  }

  /* Entrance Animation (Slide in from bottom-right) */
  @keyframes slide-in-br {
    from {
      opacity: 0;
      transform: translate(100px, 100px);
    }
    to {
      opacity: 1;
      transform: translate(0, 0);
    }
  }

  #whatsapp-float-btn {
    animation: slide-in-br 0.8s ease-out 1s backwards;
  }

  /* Pulsing Glow Effect */
  @keyframes spiritual-glow {
    0%, 100% {
      box-shadow: 0 0 20px rgba(37, 211, 102, 0.5), 0 0 40px rgba(243, 156, 18, 0.3);
    }
    50% {
      box-shadow: 0 0 30px rgba(37, 211, 102, 0.8), 0 0 60px rgba(243, 156, 18, 0.5);
    }
  }

  .whatsapp-float:hover > div {
    animation: spiritual-glow 2s ease-in-out infinite;
  }

  /* Mobile Optimization */
  @media (max-width: 640px) {
    .whatsapp-float {
      bottom: 1rem;
      right: 1rem;
    }

    /* Hide tooltip on mobile (too small) */
    .whatsapp-float > div:nth-child(3) {
      display: none;
    }
  }

  /* Tablet Optimization */
  @media (min-width: 641px) and (max-width: 1024px) {
    .whatsapp-float {
      bottom: 1.5rem;
      right: 1.5rem;
    }
  }
</style>

<script>
  // Track WhatsApp button clicks (analytics placeholder)
  document.addEventListener('DOMContentLoaded', () => {
    console.log('[WhatsAppButton] WhatsApp floating button initialized');
    
    const whatsappBtn = document.getElementById('whatsapp-float-btn');
    
    if (whatsappBtn) {
      whatsappBtn.addEventListener('click', () => {
        console.log('[WhatsAppButton] WhatsApp button clicked');
        
        // Analytics tracking (placeholder - replace with actual analytics)
        if (typeof gtag !== 'undefined') {
          // Google Analytics event tracking
          gtag('event', 'click', {
            'event_category': 'WhatsApp',
            'event_label': 'Floating Button',
            'value': 1
          });
        }
        
        // Show a brief confirmation (optional)
        const originalContent = whatsappBtn.innerHTML;
        const confirmIcon = '<div class="flex items-center justify-center w-16 h-16 md:w-18 md:h-18"><svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg></div>';
        
        // Briefly show checkmark (optional visual feedback)
        // Uncomment if you want this feature:
        // whatsappBtn.innerHTML = confirmIcon;
        // setTimeout(() => {
        //   whatsappBtn.innerHTML = originalContent;
        // }, 500);
      });
      
      // Add entrance animation class after delay
      setTimeout(() => {
        whatsappBtn.classList.add('entrance-complete');
      }, 2000);
    }
    
    console.log('[WhatsAppButton] ✓ WhatsApp button ready');
  });
</script>

